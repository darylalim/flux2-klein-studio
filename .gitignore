# Environment
.env
.venv/

# Python
__pycache__/

# Tooling caches
.pytest_cache/
.ruff_cache/

# Streamlit (config.toml is tracked; never commit secrets)
.streamlit/secrets.toml

# Claude Code (settings.json + hooks/ are tracked; personal overrides are not)
.claude/settings.local.json

# macOS
.DS_Store
